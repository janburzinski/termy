{
  "schema_version": 1,
  "id": "example.hello",
  "name": "Hello Plugin",
  "version": "0.1.0",
  "description": "Minimal TypeScript Termy plugin compiled with Bun",
  "runtime": "executable",
  "entrypoint": "./hello-plugin",
  "autostart": true,
  "permissions": ["notifications"],
  "contributes": {
    "commands": [
      {
        "id": "example.hello.run",
        "title": "Run Hello",
        "description": "Show a sample toast from the TypeScript example plugin"
      }
    ]
  }
}
