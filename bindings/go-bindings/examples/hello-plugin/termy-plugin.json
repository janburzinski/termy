{
  "schema_version": 1,
  "id": "example.go.hello",
  "name": "Hello Plugin (Go)",
  "version": "0.1.0",
  "description": "Minimal Go plugin example",
  "runtime": "executable",
  "entrypoint": "./hello-plugin",
  "autostart": true,
  "permissions": ["notifications"],
  "contributes": {
    "commands": [
      {
        "id": "example.go.hello.run",
        "title": "Hello (Go)"
      }
    ]
  }
}
