[package]
name = "plugin_release_notes"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
termy_plugin_core = { path = "../plugin_core" }
termy_plugin_sdk = { path = "../plugin_sdk" }
ureq = { version = "2", features = ["json"] }
