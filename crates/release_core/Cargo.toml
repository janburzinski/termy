[package]
name = "termy_release_core"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1"
ureq = { version = "2", features = ["json"] }
serde = { version = "1", features = ["derive"] }
semver = "1"
